spring:
  application:
    name: franchise-api
  
  # Conexión a MongoDB Atlas
  data:
    mongodb:
      uri: mongodb+srv://yersonluna:Sonyer232606.@cluster0.hcd69qv.mongodb.net/franchise_db?retryWrites=true&w=majority&appName=Cluster0

  # Importante: Desactivamos el soporte de Docker Compose para evitar conflictos
  docker:
    compose:
      enabled: false

  webflux:
    base-path: /api

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    com.franchise: DEBUG